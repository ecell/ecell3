#!/usr/bin/env python

import os
import sys
from glob import glob
from distutils.core import setup, Extension

resources = glob(
    os.path.join( os.path.dirname( __file__ ), 'resources', '*' ) )

plugins = glob(
    os.path.join( os.path.dirname( __file__ ), 'plugins', '*' ) )

setup(
    name = 'ecell.osogo',
    version = @ECELL_VERSION_STRING@,
    description = 'E-Cell Osogo -- Session Monitor',
    author = 'E-Cell project',
    author_email = 'info@e-cell.org',
    url = 'http://www.e-cell.org/',
    packages = [ 'ecell.osogo' ],
    scripts = [ 'ecell3-session-monitor' ],
    data_files = [
        ( os.path.join( os.environ['sysconfdir'], '@ECELL_DIRNAME@' ),
          [ 'osogo.ini' ] ),
        ( os.path.join( os.environ['datarootdir'], '@ECELL_DIRNAME@',
            'session-monitor', 'glade' ), resources ),
        ( os.path.join( os.environ['libdir'], '@ECELL_DIRNAME@',
            'session-monitor', 'plugins' ), plugins ),
        ]
    )
