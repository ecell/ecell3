import os

OSOGO_PATH = os.environ['OSOGOPATH']

# where to place .glade files for main (non-plugin) windows
GLADEFILE_PATH = OSOGO_PATH

# where to look at when loading plug-ins
PLUGIN_PATH = [ OSOGO_PATH + os.sep + 'plugins', '.' ]
GNOME_INSTALLED = '@GNOME_INSTALLED@'

