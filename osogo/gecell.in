#!@SHELL@

prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@

export OSOGOPATH=@libdir@/@PACKAGE@
export PYTHONPATH="${OSOGOPATH}:${PYTHONPATH}"

exec ${bindir}/ecell3 ${OSOGOPATH}/opengui.py $*

